import{performance}from"node:perf_hooks";import{evaluateDraft}from"../core/evaluate.js";import{normalizeSettings,snakeSlot}from"../shared/domain.js";
const positions=["QB","RB","WR","TE","K","DST"],players=Array.from({length:400},(_,i)=>({id:`p${i}`,name:`P${i}`,position:positions[i%6],team:`T${i%32}`,mean:320-i*.55,ceiling:380-i*.45,floor:180-i*.35,risk:.2+(i%7)/12,scarcity:.4+(i%3)/10,adp:i+1,adpSd:18,eligibleForRecommendation:true})),settings=normalizeSettings({teams:12,rounds:15,playoffTeams:6,slots:{QB:1,RB:2,WR:2,TE:1,FLEX:1,K:1,DST:1,BENCH:6}}),picks=players.slice(0,60).map((player,i)=>({pickNo:i+1,playerId:player.id,slot:snakeSlot(i+1,12)})),state={platform:"fixture",draftId:"benchmark-400",dataQuality:"experimental",settings,picks,players,updatedAt:Date.now()},results=[];
for(const filter of [undefined,["QB"],["WR"],["DST"],["QB"]]){state.updatedAt=Date.now();const start=performance.now(),recommendations=evaluateDraft({state,userSlot:7,positions:filter,iterations:120});results.push({filter:filter?.join(",")||"ALL",milliseconds:Number((performance.now()-start).toFixed(1)),cards:recommendations.length,titleChance:recommendations[0]?.teamSimulation.championshipProbability})}
console.log(JSON.stringify(results,null,2));
