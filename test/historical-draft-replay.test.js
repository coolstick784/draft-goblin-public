import test from "node:test";import assert from "node:assert/strict";import fs from "node:fs";import{replayHistoricalDrafts}from"../scripts/replay-historical-drafts.js";
const record=(season,flip=false)=>({season,teams:4,scoringType:"ppr",championSlot:flip?4:1,actualRankBySlot:flip?[4,3,2,1]:[1,2,3,4],picks:Array.from({length:32},(_,i)=>({pickNo:i+1,slot:Math.floor(i/4)%2?(4-i%4):(i%4+1),position:["RB","WR","QB","TE"][i%4]}))});
test("historical draft replay preserves season-separated calibration and full ranks",()=>{const report=replayHistoricalDrafts({records:[record(2023),record(2023,true),record(2024),record(2024,true),record(2025),record(2025,true)]});assert.equal(report.records.train,2);assert.equal(report.records.validation,2);assert.equal(report.records.test,2);assert.equal(report.holdoutReplays[0].teamResults.length,4);assert.ok(report.holdoutReplays[0].teamResults.every(x=>Number.isFinite(x.projectedRank)&&Number.isFinite(x.actualRank)));assert.equal(report.uncertainty.draws,100000)});
