import test from "node:test"; import assert from "node:assert/strict"; import { scoreSources, sourceWeights, selectedSourceWeights, ensemblePlayers, parseProjectionCsv } from "../core/sources.js";
test("ranks sources by out-of-sample error and weights the better source",()=>{const rows=[];for(let i=0;i<120;i++){rows.push({sourceId:"accurate",projected:10,actual:11,snapshotAt:1,kickoffAt:2},{sourceId:"noisy",projected:20,actual:11,snapshotAt:1,kickoffAt:2})}const scores=scoreSources(rows),weights=sourceWeights(scores);assert.equal(scores[0].sourceId,"accurate");assert.ok(weights.accurate>weights.noisy)});
test("source scoring rejects information after kickoff",()=>assert.throws(()=>scoreSources([{sourceId:"x",projected:1,actual:1,snapshotAt:3,kickoffAt:2}]),/leakage/));
test("user-selected experts are equally weighted without automatic ranking",()=>assert.deepEqual(selectedSourceWeights([{sourceId:"a"},{sourceId:"b"},{sourceId:"c"}],["c","a"]),{c:.5,a:.5}));
test("parses and ensembles projection snapshots",()=>{const rows=parseProjectionCsv("playerId,name,position,mean,floor,ceiling,adp\np1,One,RB,100,70,130,5","open",1);const players=ensemblePlayers(rows,{open:1});assert.equal(players[0].mean,100);assert.equal(players[0].position,"RB")});
