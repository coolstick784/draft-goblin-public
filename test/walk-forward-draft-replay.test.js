import test from"node:test";import assert from"node:assert/strict";import{walkForwardDraftReplay}from"../scripts/walk-forward-draft-replay.js";
const record=(season,flip=false)=>({season,teams:4,scoringType:"ppr",championSlot:flip?4:1,actualRankBySlot:flip?[4,3,2,1]:[1,2,3,4],picks:Array.from({length:24},(_,i)=>({pickNo:i+1,slot:Math.floor(i/4)%2?4-i%4:i%4+1,position:["RB","WR","QB","TE"][i%4]}))});
test("walk-forward replay evaluates multiple unseen seasons chronologically",()=>{const records=[];for(const season of [2019,2020,2021,2022])records.push(record(season),record(season,true));const report=walkForwardDraftReplay({records},{firstTestSeason:2021});assert.deepEqual(report.testSeasons,[2021,2022]);assert.equal(report.folds[0].validationSeason,2020);assert.deepEqual(report.folds[1].trainingSeasons,[2019,2020]);assert.ok(Number.isFinite(report.seasonStability.rankMaeSd))});
